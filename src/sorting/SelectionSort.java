package sorting;

public class SelectionSort {

    /*
      [3,1,2,5,4] => [1,3,2,5,4] => [1,2,3,5,4]
      [1,2,3,5,4] => [1,2,3,4,5]

      input: array A
       0       n-1
      [3,1,2,5,4]
           ^
           i
      for i in range (0,n-1)
          minIndex = i;
          for i in range(i+1,n-1)
            if A[i] < A[minIndex]
               minIndex = i;
          end
             swap(A[i],A[minIndex])
      end
     */
}
